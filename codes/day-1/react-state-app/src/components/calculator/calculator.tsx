type CalculatorPropType = {
    a: number,
    b: number
}
const Calculator = (props: Readonly<CalculatorPropType>) => {
// write calculator component

    return (
        <div>
            
        </div>
    )
}

export default Calculator